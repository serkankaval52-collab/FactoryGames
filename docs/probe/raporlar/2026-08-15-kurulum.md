# AŞAMA -2 — ORTAM KURULUMU (mimar kopyası, maskeli)

**Koşu tarihi (UTC):** 2026-08-15
**Koşu anındaki taban:** `arena/019fcd97-factorygames@888720a` (etiket `v1.3.3`)
**Bu kopyanın üretim tabanı:** `arena/019fcd97-factorygames@b6338fa` (etiket `v1.3.5`)
**Executor pini:** Claude Opus 5 (1M bağlam) — `claude-opus-5[1m]` · uzantı
`anthropic.claude-code@2.1.233` · VS Code `1.133.0`
**Makine:** `<makine>` — Windows 10 Pro (10.0.19045) — AMD64
**Fabrika dizini:** `<ev>\Desktop\FactoryGames`

## SONUÇ

**OTOMATİK TAMAM** — tablodaki 11 satırın tamamı doğrulandı, BAŞARISIZ satır yok, açık kapı yok.

## T_kurulum

| damga | ham (epoch ms) | insan-okur (UTC) |
|---|---|---|
| kurulum-start | `1786764946747` | 2026-08-15 03:35:46 |
| kurulum-end | `1786767031036` | 2026-08-15 04:10:31 |

Fark: `(1786767031036 − 1786764946747) / 1000 = 2084.289 sn` → **34 dk 44 sn**
(kapı beklemeleri dahil; kapılar T_uretim'e girmez).

## Araç tablosu

| # | araç | komut (maskeli) | beklenen | gerçekleşen | durum |
|---|---|---|---|---|---|
| 1 | winget | `& "<ev>\AppData\Local\Microsoft\WindowsApps\winget.exe" --version` | `v1.*` | `v1.29.280` | ✅ kurulu (önceden) |
| 2 | Git | `git --version` | `git version 2.*` | `git version 2.53.0.windows.2` | ✅ teyit |
| 3 | GitHub CLI | `gh --version` ; `gh auth status` | `2.*` ; Logged in | `gh version 2.92.0 (2026-04-28)` ; `✓ Logged in to github.com account serkankaval52-collab (keyring)` | ✅ kurulu (önceden) |
| 4 | Python | `py -3.12 --version` | `Python 3.12.*` | `Python 3.12.10` | ✅ kurulu (önceden) |
| 5 | Unity Hub | `"C:\Program Files\Unity Hub\Unity Hub.exe" -- --headless help` | yardım metni | exit **0** + tam yardım metni | ✅ kurulu (önceden) |
| 6 | Unity pini | `"Unity Hub.exe" -- --headless editors --installed` | kurulu 6000.3 varsa pin odur | `2022.3.62f3`, **`6000.3.16f1`** → pin = 6000.3.16f1, arşive gidilmedi | ✅ kilit kuruldu |
| 7 | Unity + Android + lisans | `Unity.exe -batchmode -quit -projectPath <ev>\Desktop\FactoryGames\_probe-unity -logFile -` | çıkış kodu 0 + lisans satırı | exit **0**; SDK 811 MB / NDK 2 252 MB / JDK 228 MB | ✅ gate geçti |
| 8 | Resmî Unity MCP köprüsü | `relay_win.exe --mcp --project-path <gecici-proje>` → `initialize` → `tools/list` | handshake + araç listesi; sahne-düzenleme kapalı/yok | handshake OK; **7 araç → 6 araç** (`Unity_RunCommand` kapatıldı); kilit serbest; geçici proje silindi | ✅ doğrulandı |
| 9 | VS Code + executor | `code --version` | sürüm satırı | `1.133.0` / `a5b5009…` / `x64` | ✅ teyit |
| 10 | FFmpeg | `ffmpeg -version` | `ffmpeg version *` | `ffmpeg version 8.1.2-essentials_build-www.gyan.dev` | ✅ kurulu (önceden) |
| 11 | gcloud + Test Lab | `gcloud firebase test android models list` | model tablosu | exit **0**, **206 satır** model tablosu | ✅ doğrulandı |

## İnsan kapıları

Ön-bildirim koşunun ilk işi olarak yapıldı (kesin: ≥2 UAC + satır 11 tarayıcı; koşullu:
hesaplar, satır 8 ağ/AI). **Sürpriz kapı çıkmadı.** Fiilen açılan 3 kapı:

| satır | kapı | sonuç |
|---|---|---|
| 8 | web erişimi (doküman okuma) | verildi → resmî Unity MCP dokümanı okundu |
| 8 | Editor UI'da `Unity_RunCommand` kapatma | yapıldı → araç listesinden düştü (kanıt aşağıda) |
| 11 | `gcloud auth login` tarayıcı onayı | `<hesap>` ile giriş |

Ön-bildirilip **gerekmeyen** kapılar: satır 1 Store girişi, satır 3 `gh auth login`
(zaten oturumlu), satır 4 Python, satır 5/7 UAC (Hub + Editor önceden kurulu), satır 6 ağ
(pin kurulu sürümden okundu), satır 8 Unity hesabı, satır 10 PATH.

---

## Kritik alan çift-kayıt (S3 genelgesi)

### Lisans

**Ham satırlar** — `C:\ProgramData\Unity\Unity_lic.ulf`, `Select-String -Pattern 'Date'`:
```
<StartDate Value="2022-11-30T00:00:00"/>
<UpdateDate Value="2026-08-16T03:51:43"/>
<InitialActivationDate Value="2022-11-30T12:08:05"/>
```
Ham dosya damgası: `LastWriteTimeUtc = 2026-08-15 03:51:44`
Ham (maskeli): `<LicenseVersion Value="6.x"/>` · `<SerialMasked Value="<lisans>"/>`
Aynı alanın koşu içindeki ilk okuması: `<UpdateDate Value="2026-08-16T03:41:50"/>`
(dosya damgası `2026-08-15 03:41:50Z`).

**Çıkarım:** `UpdateDate` geçmiş değil **vade**dir — her iki ölçümde de dosya yazma anından
tam **+24 saat − 1 sn** (`03:41:50→03:41:50+1g`, `03:51:44→03:51:43+1g`). Personal lisansın
24 saatlik yenileme döngüsü. Lisansın **canlı** olduğunun kanıtı bu alan değil, ULF'nin
T_kurulum penceresi içinde **iki kez yeniden yazılmış olmasıdır** (`03:41:50Z` ve
`03:51:44Z`; pencere `03:35:46–04:10:31Z`). İkinci yazma satır 8'in Editor oturumundan geldi.
Serinin `F4-` öneki Personal'a işaret eder (seri maskelendi).

### Sürüm / pin

**Ham (üç bağımsız kaynak):**
```
(Get-Item "…\6000.3.16f1\Editor\Unity.exe").VersionInfo.ProductVersion
  → 6000.3.16f1_a56f230f6470
gate log → Initialize engine version: 6000.3.16f1 (a56f230f6470)
ProjectVersion.txt → m_EditorVersionWithRevision: 6000.3.16f1 (a56f230f6470)
```
Karşılaştırma kaydı: `2022.3.62f3` → `2022.3.62f3_96770f904ca7` (biçim doğrulaması).
Pin dosyası içeriği: `6000.3.16f1 — a56f230f6470`

**Çıkarım:** Kurulu bir 6000.3 bulunduğu için pin odur; resmî arşive hiç gidilmedi.
Tablonun birincil kaynağı olan `modules.json` bu makinede **başka sürümün** release verisini
taşıyordu (ham kanıt: `"slug":"6000.4.9f1-windows-x86_64-visualstudio"`), bu yüzden birincil
kanıt `Unity.exe` ProductVersion'a taşındı. → mimar kararı 5, `kurulum.md` v1.3.4'te uygulandı.

### Çıkış kodları (gate'ler)

**Ham:**
```
satır 7  : Exiting batchmode successfully now!
           Exiting without the bug reporter. Application will terminate with return code 0
satır 8  : {"result":{"protocolVersion":"2024-11-05","serverInfo":{"name":"unity-mcp-server",
           "version":"1.0.0"}},"jsonrpc":"2.0","id":1}
           connection.established connectionType=Named Pipe
satır 11 : EXIT=0 ; çıktı 206 satır (model tablosu)
py -3.12 tools/arac_suzgec.py   → "A1.2 arac suzgeci YESIL: 37 dosya, 10 arac oruntusu
                                   tarandi; eksik satir yok."  EXIT=0
py -3.12 tools/esik_kapsama.py  → "Esik kapsama YESIL: Ek C Bolum-1'deki 55 alanin tamami
                                   ilk-kosu.md'de geciyor."     EXIT=0
```
**Çıkarım:** Satır 7'nin gate'i (batchmode exit 0) sağlandı; listeleme yalnız ara kanıt
olarak kullanıldı. Satır 11'de tablonun **gelmesi** yetki + API-açık kanıtıdır. Bu kopyanın
push öncesi iki zorunlu süzgeci de yeşildir.

### Damga / zaman

**Ham:** `kurulum-start.ts = 1786764946747` · `kurulum-end.ts = 1786767031036`
Başlangıç damgası Python'suz atıldı (`[DateTimeOffset]::UtcNow.ToUnixTimeMilliseconds()`),
bitiş `py -3.12 tools/probe/marker.py kurulum-end` → `ok: docs/probe\.markers\kurulum-end.ts`
**Çıkarım:** İkisi de TAM SAYI epoch ms; kültür-bağımsız (tr-TR virgülü girmedi).
Fark 2084.289 sn = 34 dk 44 sn.

### Kimlik

**Ham:** `git branch --show-current` → `arena/019fcd97-factorygames` ·
`git log --oneline -1` → `888720a …` · `git describe --tags --exact-match HEAD` → `v1.3.3`
Executor: `anthropic.claude-code@2.1.233`, VS Code `1.133.0`, model `claude-opus-5[1m]`
Bellek dizini: **boş** (sıfır oturum, önceki bağlam yok).
**Çıkarım:** Ön-durum birebir tuttu; koşu boyunca dal değişmedi, commit/push yapılmadı.

---

## Satır 8 — köprü kanıtı (ayrıntı)

**Okunan resmî doküman:** `docs.unity3d.com/Packages/com.unity.ai.assistant@2.10/manual/
integration/unity-mcp-get-started.html` · `…@2.11/manual/reference/unity-mcp-reference.html`
· `…@2.0/manual/mcp-configure.html` · sürüm kaynağı `packages.unity.com/com.unity.ai.assistant`

**Uygulanan:** `manifest.json` → `"com.unity.ai.assistant": "2.17.0-pre.1"` → batchmode
çözümleme exit 0 (`Registered 42 packages: com.unity.ai.assistant@2.17.0-pre.1`) → relay
ikilisi `<ev>\.unity\relay\relay_win.exe` (100 732 336 bayt) → **EDITOR OTURUMU executor
tarafından komutla başlatıldı** (insan kapısı değil) → relay `--mcp` ile JSON-RPC sorgusu.

**Araç listesi — kapatma öncesi (7):** `Unity_GetConsoleLogs`, `Unity_Camera_Capture`,
`Unity_SceneView_Capture2DScene`, `Unity_SceneView_CaptureMultiAngleSceneView`,
`Unity_AssetGeneration_GetModels`, `Unity_AssetGeneration_GenerateAsset`, `Unity_RunCommand`

**Araç listesi — kapatma sonrası (6, ham çıktı):**
```
ARAC SAYISI: 6
 - Unity_AssetGeneration_GenerateAsset
 - Unity_AssetGeneration_GetModels
 - Unity_Camera_Capture
 - Unity_GetConsoleLogs
 - Unity_SceneView_Capture2DScene
 - Unity_SceneView_CaptureMultiAngleSceneView
```
Adı sahne-düzenleme olan araç (`Unity_ManageScene` vb.) bu sürümde **hiç sunulmuyor**.
`Unity_RunCommand` ise kendi açıklamasında *"compile and execute a C# script… control
virtually every aspect of the game"* diyordu — adı farklı olsa da sahne-düzenleme yüzeyi
oydu, bu yüzden kapatıldı. İki deneme reddedildi (`System.Reflection` namespace yasak;
`GameObject` oluşturma → `Execution failed`) → "kısıtlı ama kapalı değil" kararı.

**Kalıcılık (ham, EditorPrefs `HKCU\Software\Unity Technologies\Unity Editor 5.x` →
`Unity.AI.MCP.ProjectSettings.v2`):**
```json
{ "bridgeEnabled": true, "batchModeEnabled": true, "autoApproveInBatchMode": true,
  "validationLevel": "standard", "processValidationEnabled": true,
  "connectionPolicies": { "gateway": {"allowed": true, "requiresApproval": false},
                          "direct":  {"allowed": true, "requiresApproval": true} },
  "enabledToolOverrides": [], "disabledToolOverrides": ["Unity_RunCommand"] }
```
**Çıkarım:** ayar **kullanıcı genelinde** saklanıyor — geçici proje silindikten sonra tekrar
okundu, `Unity_RunCommand` hâlâ `disabledToolOverrides` içinde. Paketin kendisi ise projeye
bağlıdır (gerçek projede manifest'e ayrıca eklenmeli → mimar kararı 7).

**Hesap/AI yetkisi (tablodaki "BİLİNMİYOR" maddesinin cevabı) — ham log:**
`Found 0 entitlement groups and 0 free entitlements matching requested entitlement ids` (404),
`Access token is unavailable`.
**Çıkarım:** Unity hesabı oturumu ve AI yetkisi **yok**; buna rağmen köprü çalıştı. Yani
**köprü hesap gerektirmiyor**, yalnız `Unity_AssetGeneration_*` araçları gerektiriyor.
Kapı açılmasına gerek kalmadı.

**Kapanış:** Editor executor tarafından kapatıldı → `Temp/UnityLockfile` **kayboldu** →
geçici proje silindi → `git status --short` yalnız kurulum çıktılarını gösterdi.

---

## Bekleyen iş / karar durumu

Mimar kararları 2026-08-15'te alındı; numaralar birebir eşleşir.

| # | konu | durum |
|---|---|---|
| 1 | Üçüncü taraf MCP köprüleri (`MCPForUnity`, `McpUnity`) makinede kurulu, `manage_scene`/`execute_code`/`batch_execute` dahil **tüm araçlar `=1`** | **KAPANDI** — fabrika koşusunda yalnız resmî köprü; üçüncü tarafa bağlanılmaz (kural 30, v1.3.4). Kullanıcının kurulumuna dokunulmaz. Muhtemel kaynak: makinede kurulu ikinci AI uzantısı `saoudrizwan.claude-dev@4.1.10` (Cline) |
| 2 | `autoApproveInBatchMode: true` | **ERTELENDİ** — 0B'de paket kurulurken aynı UI oturumunda `false` + kanıt |
| 3 | `gateway.requiresApproval: false` | **ERTELENDİ** — madde 2 ile aynı oturumda `true` |
| 4 | `gh` token scope'ları `gist, read:org, repo, workflow` | **DÜŞÜK ÖNCELİKLİ İNSAN İŞİ** — `repo`+`workflow` kapsanıyor, hat kilitli değil |
| 5 | changeset birincil kaynağı | **UYGULANDI** (v1.3.4) |
| 6 | `ELECTRON_RUN_AS_NODE` süreç temizliği | **UYGULANDI** (v1.3.4) |
| 7 | `com.unity.ai.assistant` pini `2.17.0-pre.1` | **DEVREDİLDİ** — 0A adım 3 şablon manifesti; 0B'de araç listesi + RunCommand-kapalı kanıtı yeniden alınır |

## Yerinde düzeltmeler ve şerhler

1. **ULF `UpdateDate` yorumu düzeltildi (mimar bulgusu, 2026-08-15).** İlk raporda bu alan
   "gate koşusu lisansı yeniledi" kanıtı olarak gösterilmişti; alan aslında 24 saatlik
   **vade**dir. Kanıt `LastWriteTimeUtc`'ye taşındı. Sonuç değişmedi, dayanak düzeldi.
2. **`AndroidPlayer` ölçüm tuzağı.** Kök dizin ilk ölçümde 9,86 MB göründü; alt dizin
   taraması yapılmasaydı "eksik kurulum" diye yanlış karar verilecekti. Gerçek: SDK 811 MB,
   NDK 2 252 MB, JDK 228 MB.
3. **`ELECTRON_RUN_AS_NODE=1` sızıntısı.** VS Code kaynaklı bu değişken Unity Hub'ı
   (Electron) node olarak başlatıp `Error: Cannot find module '--headless'` verdiriyordu.
   Süreç düzeyinde temizlendi; **sistem değişkeni kalıcı değiştirilmedi**.
4. **winget PATH'te değil** (`WindowsApps` bu oturumun PATH'inde yok) → tam yolla çağrıldı.
   Ayrıca `msstore` kaynağı sözleşme onayı istediği için sorgular `--source winget` ile
   sınırlandı; **kullanıcı adına Store sözleşmesi kabul edilmedi**.
5. **gcloud PATH'i bu oturuma akmadı** (yükleyici kullanıcı/makine PATH'ini güncelledi) →
   tam yolla çağrıldı. Yeni terminalde doğrudan çalışır.
6. **Maske şerhi.** Maske haritası "ULF satırı → `<lisans>`" diyor; S3 çift-kayıt ise lisans
   alanında ham satır istiyor. Kimlik taşıyan alan (`SerialMasked`) maskelendi, kimlik
   taşımayan tarih satırları **ham** bırakıldı — vade aritmetiğinin kanıtı onlar. Mimar
   aksini isterse tarih satırları da maskelenir. Ayrıca haritada karşılığı olmayan ikinci
   GitHub hesabının adı ihtiyaten `<hesap>` sayıldı; aktif hesap adı repo URL'sinde zaten
   public olduğu için bırakıldı.

## Yetki sınırı uyumu

Fabrika dizini dışında dosya oluşturulmadı/silinmedi/değiştirilmedi (tek geçici proje
fabrika dizini altındaydı, silindi). Tablo dışı yazılım kurulmadı; hiçbir yazılım
kaldırılmadı/sürümü düşürülmedi. Sistem ortam değişkeni kalıcı değiştirilmedi. Defender'a
dokunulmadı. Kayıt defterine executor yazmadı (`Unity.AI.MCP.*` anahtarını Unity Editor
yazdı). Kullanıcının mevcut Unity projelerine ve mevcut oyun kaynağına erişilmedi.
GitHub'da yeni yetki istenmedi; ikinci hesaba dokunulmadı. gcloud yetkisi yalnız Test Lab
kapsamında kullanıldı, **fatura hesabı kurulmadı**, mevcut 3 buluta projesine dokunulmadı,
yeni proje `<proje>` olarak açıldı. Koşu boyunca commit/push yapılmadı; dal değiştirilmedi.
