# AŞAMA 7 — Kontrol ve Temizlik

**Girdi:** içerik-tam build.
**Çıktı:** hata raporu (açık kritik = 0); min-spec cihaz performans raporu —
CİHAZ KOŞUSU (Firebase Test Lab, Ek C `min_spec_testlab_pin`): premium-sozlesme
P1 tepki kare sayısı + P3 geçiş süreleri + P6 p95 / p99−p50 (FrameTimingManager)
+ P8 soğuk açılış + P9(b) ses gecikmesi; kanıt koşu-APK'sının uygulama-içi damga
kaydıdır (A2.2 — batchmode bu ölçümleri ÜRETMEZ; batchmode bot koşusu mantıkla
sınırlıdır: soak 0 exception, boot imza). Her cihaz koşusu Ek C
`testlab_gunluk_kota_fiziksel` kotasından düşer; politika
ön kontrol listesi (`docs/standards/red-flag.md`) PASS — **OTOMATİK maddelerin
tamamı CI/bot'ta kanıtlı** (sayısal sınırlar Ek C'den); İNSAN maddeleri (R2b, R4)
Aşama 8'in ikili listesine taşınır, bu aşamada bekleme YOK — 4.3 oyun seviyesi
**ve** hesap-deseni envanteriyle karşılaştırma (mevcut oyun kaydı dahil) sürer;
analitik şema kanıtı (şemadaki her olay ≥1 kez loglandı; `sessiz_toparlanma`
dahil — P5); screenshot paketi —
yalnız gerçek build'den, aynı koşunun artefaktı (R7; Aşama 8 raporu içindir,
**kapı değil**);
**mağaza taslak kaydı + temel test paketi** (ASC app kaydı + iOS imzalı test IPA'sı
TestFlight internal'a — zincir 0B'den kalır; iOS'lu dış göz yoksa bu kalem pas).
**Yürüten:** otonom.
**Geçiş kriteri:** kontrol listesinin tamamı yeşil; CI kapısı (lint — görsel
sözleşme G1–G4/G6/G7 + ses S1 + premium P2/P4/P5/P7/P9(a) + yerelleştirme
(gömülü-metin — A3.6) dahil, secret-scan, testler, boot imza) yeşil; cihaz koşusu raporu yazıldı.
**Geri kenarı:** kırmızı madde kaynak düzlemine döner: oyun kodu → Aşama 4/6;
şablon/CI → FactoryGames arıza issue'su + Sözleşme-5 yükseltmesi; **max 4 tur**
(cihaz koşusu gerektiren turlar kotadan düşer); sonra insana.
