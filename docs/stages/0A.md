# AŞAMA 0A — Hat Kurulumu (tek seferlik)

**Girdi:** geçilmiş sonda (önkoşulu: `stages/kurulum.md` kapalı); mevcut hesaplar
(bireysel Play + Apple, ikisinde de yayında oyun, aktif AdMob); kullanıcının Ek C
Bölüm-1 form dönüşü.

Sıra kasıtlıdır: en yüksek belirsizlikli kapı (C+ sonrası iOS ayağı) en ucuz yolla
ÖNCE test edilir (L1). Her adımın kanıtı METİNDİR (Sözleşme-4): görüntü/ham artefakt
cihazda kalır, repoya `{tarih, kim, sonuç}` satırları girer. **Makine ilkesi (C+,
v1.4.0):** CI runner'ında Unity KOŞMAZ — Personal CI'da resmen lisanslanamaz (kanıt:
`verification/06-lisans.md`); Unity işleri yerelde, kanıtı metin olarak repoda.

## Adımlar

1. **MAĞAZA + FATURA TEYİDİ** (insan, ~35 dk, GÜN 1): Play'de yeni taslak →
   Üretim sekmesi açık mı (12×14); ASC'te yeni app kaydı açılabiliyor mu; GitHub
   Billing'de public-runner satırı + Actions tüketim sayfası. Kanıt: tek metin
   dosyası `docs/factory/verification/01-02-hesaplar.md`. Teyit yoksa DUR —
   hesap gündemi yeniden açılır.
2. **iOS AYAĞI YEREL KANITI — İLK KAPI** (executor, ~1 sa; Unity-CI lisans kapısı C+
   kararıyla KAPANDI — kanıt/geçmiş `verification/06-lisans.md`): (a) Pinli editöre
   **iOS Build Support modülü**: Hub `--headless install-modules --version <pin>
   --module ios` (kurulum tablosunda yoktu; makine değişikliği bu aşamanın yetkisindedir,
   kanıt modül listesi). (b) Geçici boş projede batchmode `-executeMethod` ile **Xcode
   projesi üretimi**: exit 0 + `*.xcodeproj` var = KANIT → `verification/02-ios-uretim.md`;
   proje silinir. Doğrulama "Windows'tan çıkmıyor" derse DUR — UBA'ya düşülür (H4 kalıntısı).
3. **ŞABLON İSKELET** (otonom): `factory.core` **ayrı public repo** (L4) — UPM
   paketi (RuntimeInitBootstrap, JsonLoader, TelemetryWriter, analitik/reklam
   wrapper'ları; oyun bilgisi YOK; sürüm tag'li). `templates/` = oyun reposu
   iskeleti: presets kopyası, değiştirilmemiş şablon sahnesi, sondan gelen
   **`scene-baseline.json`** (oyun reposu P1 lint'inin referansı — şablona gömülür),
   `StreamingAssets/ config.json` iskeleti, 3 workflow — Unity'siz (C+): lint +
   secret-scan (kapsamın tek kaynağı Aşama 7 kapı satırı: G1–G4/G6–G8 + S1 + premium
   başsız + dil kapısı A3.6), saf-C# dotnet test (denge_sim dahil), ios xcodebuild
   (adım 4 düzeneği; Android kanıtı yerel batchmode+metin), CLAUDE.md (PIPELINE referansı, ≤30 kural). UPM bağımlılığı git URL +
   `#tag` (public repo → CI'da token gerekmez). Bu repo için: `ci/factory-gates.yml`
   executor'ın user-auth'uyla `.github/workflows/` altına kopyalanır (scope'ta
   `workflow` var). **Eşik sağlaması (Belge 1/V3 — kullanıcı onayı koşulu):** G1–G8/S1
   lint'leri kullanıcının YAYINDAKİ oyununun erişilebilen varlıklarına SALT-OKUR koşulur
   (değişiklik yok); geçme oranı metni `verification/` altına girer — eşikler gerçekçi
   mi ilk oyundan ÖNCE görülür; oran çok düşükse eşikler Sözleşme-5 gündemi olur.
4. **HELLO-BUILD** (karma): şablondan `factorygames-hello` reposu push'lanır.
   Android: YEREL batchmode → APK + kanıt metni (verification/). CI: lint + saf-test
   yeşil linki. iOS (macos runner, Unity'siz): yerelde üretilen xcodeproj zip →
   geçici Release asset'i (`gh release`; git tarihine girmez — kural 25 temiz) →
   ios job'ı indirip xcodebuild **derleme kanıtı** koşar (imza zinciri + ASC key 0B'de).
   **Yerel düzen (v1.3.2):** oyun klonları fabrika klonunun İÇİNDE değildir — iç içe
   repo yasak; hepsi tek üst dizinde kardeş klasörlerdir (fabrika + oyunlar yan yana).
   Üretim koşusunda VS Code OYUN reposunun kökünde açılır, fabrika kökü kapalıdır —
   model bağlamı fiziksel olarak da oyuna kilitlenir (kural 1/okuma kapsamıyla çift
   yalıtım); fabrika belgelerine public linkten bakılır. `factory.core` oyuna UPM
   git URL + `#tag` ile bağlanır, oyun reposuna kopyalanmaz.
5. **KASITLI İHLAL + TEMİZ-PR** (otonom, ~45 dk): geçici branch'te BEŞ kırmızı
   beklenen run: (a) 40 nesneli sahne (baseline lint); (b) sahte token'lı
   `config.cs` (secret-scan); (c) 2 MB sahte `.png` (boyut + ham-artefakt);
   (d) `EditorBuildSettings`'e ikinci sahne (tek-kaynak); (e) preset dosyasında
   elle değişiklik (preset-sapma — preset kararının tek mekanik savunması).
   ALTINCI run: **TEMİZ PR yeşil** — yanlış-pozitif testi; sürekli kırmızı
   yanan kapı ilk haftada bypass edilir. Branch silinir; 6 link kanıt.
6. **STANDART DOSYALARI** (karma, ~yarım gün): `standartlar/` — fikir standardı;
   çeşitlilik defteri (**ilk kayıt = mevcut oyun**; doldurma protokolü: kullanıcıya
   TEK form — 8 eksen + şablon-deseni blokları: ikon baskın rengi/biçim dili, ana
   menü akışı, reklam yerleşimleri format+konum, UI kiti/fontlar; ~1 dk'lık iş);
   hesap-deseni envanteri (mevcut oyun hücreleri işli); rubrik şablonu; pre-mortem
   taksonomisi; PR şablonu; `FALLBACK.md`; telemetry şeması; halka-envanteri v0.
   **Ek C Bölüm-1 formu:** kullanıcıya TEK form; ilk koşudan önce ölçülemeyecek
   alanlar "ilk koşuda ölçülecek — şimdilik tahmin" etiketiyle döner (L8). Formun
   insan-taahhüt bölümü (A6.1/A6.4): beş kapı türü için azami dönüş süresi
   (`insan_yanit_tavan_*`) + `halka_davet_tavan_gun` — kullanıcı kendisi yazar.
7. **HALKA v0 + PRIVATE İKİZ** (insan): `FactoryGames-private` repo bugün açılır —
   tester_id ↔ gerçek kişi EŞLEME tablosu ORADA (tek dosya; public repo hiç
   görmez) + envanter ve form yanıtlarının ANONİM birincil kaydı da oradadır
   (A3.5 — yerel kopya kaybolursa halkanın yorgunluk/rol geçmişi ölür; çalışma
   kopyası yerel `halka/`, gitignore'lu). Public'e yalnız özet/sayım girer.
   Davet + form metninin tek kaynağı `docs/standards/halka-standardi.md`'dir
   (KVKK cümlesi metne gömülü). v0: ≥5 kişi × 2 tur iskeleti.
8. **SOSYAL HESAPLAR** (insan): fabrika TikTok + YouTube açıldı; ad standardı
   kayıtlı. Erişimler cihazda — secrets'a taşıma ancak organik kit otomasyonu
   doğunca (Sözleşme-4 kapsamında, o gün).
9. **MCP + İKİ-DURUM TEYİDİ** (otonom, ~30 dk): şablon projede deklare EDITOR
   OTURUMU aç/kapa (kapanışta `Temp/UnityLockfile` yok kanıtı) + `FALLBACK.md`'den
   bir CLI eşdeğeri; çıktı `docs/factory/verification/09-mcp.md`.

**0A geçiş kriteri:** 1–9 tik; iOS yerel üretim kanıtı (adım 2) + ios xcodebuild
linki; CI yeşil linki + yerel APK metni; ihlal 5 + temiz-PR 1 link; defterde mevcut
oyun; Ek C Bölüm-1 form dönüşü DOLU; `FactoryGames-private` açık.
**Yürüten:** karma — insan yükü ≈ 4 sa + bekleme; takvim ≈ 1 hafta.
**Geri kenarı:** bir adım 3 denemede kapanmazsa → insana eskalasyon, hat duraklar
(tur limiti yok; tek DUR hakkı adım 2 — H4 kalıntısı UBA'ya düşer).
